<Window x:Class="TfsUtil.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cmd="clr-namespace:TfsUtil.Commands"
        Title="MainWindow" Height="350" Width="525" WindowStartupLocation="CenterScreen" FontFamily="{StaticResource DefaultFontFamily}" FontSize="{StaticResource DefaultFontSize}">
    <Window.Resources>
        
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="cmd:AppCommands.MergeSearch" CanExecute="CanExecuteMergeSearch" Executed="ExecuteMergeSearch" />
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Height="23" HorizontalAlignment="Stretch" Name="mainMenu" VerticalAlignment="Top" FontFamily="Verdana" FontSize="11">
            <MenuItem Name="FileMenu" Header="_File">
                <MenuItem Name="mergeSearchMenuItem"  Header="_Merge Search..." Command="cmd:AppCommands.MergeSearch" />
            </MenuItem>
            <MenuItem Name="ServerMenu" Header="_Server" />
        </Menu>
        <TextBox Grid.Row="1" HorizontalAlignment="Stretch" Margin="0,0,0,0" Name="logBox" VerticalAlignment="Stretch" IsReadOnly="True" IsReadOnlyCaretVisible="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
